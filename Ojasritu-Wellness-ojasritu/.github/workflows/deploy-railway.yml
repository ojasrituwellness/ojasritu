jobs:
name: Disabled - old Railway workflow

on:
  push:
    branches: [ main ]

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is intentionally disabled
        run: |
          echo "This workflow was replaced by .github/workflows/deploy.yml. Exiting."
